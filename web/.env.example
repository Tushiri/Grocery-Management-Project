# Supabase project (public — safe for the browser bundle)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# AI microservice (server-only — never exposed to the browser)
AI_SERVICE_URL=
AI_SERVICE_TOKEN=
